{
  "name": "ironlog-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Local-first desktop fitness tracking app for custom workouts, PRs, analytics, and private progress photos.",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @ironlog/desktop",
    "desktop": "npm run tauri:dev -w @ironlog/desktop",
    "mobile:dev": "npm run dev -w @ironlog/mobile-pwa",
    "mobile:build": "npm run build -w @ironlog/mobile-pwa",
    "mobile:build:pages": "npm run build:pages -w @ironlog/mobile-pwa",
    "mobile:preview": "npm run preview -w @ironlog/mobile-pwa",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
